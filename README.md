# Alchemical Agent Ecosystem

Self-hosted, local-first, open-source multi-agent platform.

## One-command setup
```bash
curl -fsSL https://smouj.ai/install.sh | bash -s -- --domain your-domain.com
```

## Local dev setup (current)
```bash
cd /mnt/d/alchemical-agent-ecosystem
bash infra/scripts/install.sh --domain localhost
```

## Exposed service paths (via Caddy)
- `/velktharion/*`
- `/synapsara/*`
- `/kryonexus/*`
- `/noctumbra-mail/*`
- `/temporaeth/*`
- `/vaeloryn-conclave/*`
- `/ignivox/*`
- `/auralith/*`
- `/resonvyr/*`
- `/fluxenrath/*`
